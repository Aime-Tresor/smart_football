DB_HOST=localhost
DB_NAME=fa_db
DB_USER=root
DB_PASSWORD=

# Which AI provider generates the card-reason / appeal-decision summary:
# anthropic (default), openai, gemini, or groq (free tier, hosts
# open-source models like Llama). Only the selected provider's key needs
# to be set below.
AI_PROVIDER=anthropic

ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-haiku-4-5

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini

GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash

GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
